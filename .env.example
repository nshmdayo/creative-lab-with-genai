# Google AI Creative Tools Environment Configuration
# Set actual values and save as .env file

# Google AI API Key (obtain from https://ai.google.dev/)
GOOGLE_AI_API_KEY=your_api_key_here

# Google Cloud Project ID
GOOGLE_CLOUD_PROJECT_ID=your_project_id_here

# Google Cloud Region (optional, default: us-central1)
GOOGLE_CLOUD_LOCATION=us-central1

# Path to Google Cloud Service Account key file (optional)
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json

# Output directory (default: ./output)
OUTPUT_DIR=./output

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# FFmpeg path (for custom installations)
# FFMPEG_PATH=/usr/local/bin/ffmpeg

# Authentication methods:
# 1. API Key Authentication (simple):
#    Set GOOGLE_AI_API_KEY
#
# 2. Service Account Authentication (recommended):
#    Set GOOGLE_CLOUD_PROJECT_ID and GOOGLE_APPLICATION_CREDENTIALS
#
# 3. gcloud CLI Authentication:
#    Run `gcloud auth application-default login`, then
#    set GOOGLE_CLOUD_PROJECT_ID
